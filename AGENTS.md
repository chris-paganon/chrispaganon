Use pnpm, not npm.

# Git

Make atomic git commits as you go, following git best practices:

- Make sure `pnpm typecheck` is passing before committing.
- Make small, focused commits.

# Project Structure

This project uses Nuxt v3, VueJS, TypeScript, and Motion V (framer motion for VueJS).
